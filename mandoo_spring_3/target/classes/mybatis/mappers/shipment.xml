<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ShipmentMapper">

	<select id="getAllShipments" resultType="ShipmentDTO">
		SELECT oi.ORDER_ID,
		c.CLIENT_NAME as CLIENT_ID,
		oi.ORDER_DATE,
		oi.ORDER_ENDDATE,
		oi.ORDER_BOX,
		oi.ORDER_COUNT,
		oi.ORDER_PRICE,
		oi.PRODUCT_NAME
		FROM ORDER_INFO oi
		JOIN CLIENT c ON oi.CLIENT_ID = c.CLIENT_ID
	</select>

	<delete id="deleteShipment">
		DELETE FROM order_info WHERE shipment_ID = #{shipment_Id}
	</delete>
</mapper>

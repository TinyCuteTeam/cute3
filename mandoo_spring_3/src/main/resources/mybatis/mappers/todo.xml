<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TodoMapper">
	<select id="getTodosByUserId" resultType="TodoDTO">
		SELECT * FROM TODO WHERE USER_ID = #{userI_d}
	</select>

	<insert id="addTodo">
		INSERT INTO TODO (TODO_ID, USER_ID, TODO_DATE, TODO_TITLE,
		TODO_CONTENTS)
		VALUES (SEQ_TODO.NEXTVAL, #{user_Id}, #{todo_Date}, #{todo_Title},
		#{todoContents})
	</insert>

	<update id="updateTodo">
		UPDATE TODO
		SET TODO_TITLE = #{todo_Title}, TODO_CONTENTS = #{todo_Contents}, TODO_DATE
		= #{todo_Date}
		WHERE TODO_ID = #{todo_Id}
	</update>

	<delete id="deleteTodo">
		DELETE FROM TODO WHERE TODO_ID = #{todo_Id}
	</delete>
</mapper>
